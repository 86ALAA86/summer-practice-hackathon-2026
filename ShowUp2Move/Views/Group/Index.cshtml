@model List<clsGroup_BLL>
@{
    Layout = "_Layout";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h3>My Groups</h3>
    <a href="/Group/RunMatching" class="btn btn-success">
        <i class="bi bi-people-fill"></i> Find a Group
    </a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">You are not in any groups yet. Use <strong>Find a Group</strong> to get matched!</div>
}
else
{
    <table class="table table-bordered table-hover">
        <thead class="table-success">
            <tr>
                <th>Sport</th>
                <th>Captain</th>
                <th>Status</th>
                <th>Created</th>
                <th>Confirmed?</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (clsGroup_BLL group in Model)
            {
                <tr>
                    <td>@group.SportName</td>
                    <td>@group.CaptainName</td>
                    <td><span class="badge bg-success">@group.Status</span></td>
                    <td>@group.CreatedAt.ToString("dd MMM yyyy")</td>
                    <td>
                        @if (group.HasConfirmed)
                        {
                            <span class="badge bg-success">Yes</span>
                        }
                        else
                        {
                            <form method="post" action="/Group/Confirm">
                                <input type="hidden" name="id" value="@group.GroupID" />
                                <button type="submit" class="btn btn-sm btn-outline-success">Confirm</button>
                            </form>
                        }
                    </td>
                    <td>
                        <a href="/Group/Detail/@group.GroupID" class="btn btn-sm btn-outline-secondary">View</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}